from django.urls import path
# from .views import manage_internal, add_score

urlpatterns = [
    # path('manage_internals/', manage_internal, name='index'),
    # path('add_score/', add_score, name='add_score'),
]